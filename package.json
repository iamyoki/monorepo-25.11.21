{
  "private": true,
  "scripts": {
    "clean": "del-cli **/dist **/*.tsbuildinfo",
    "dev": "pnpm --filter './packages/*' run -r build && pnpm run --parallel dev"
  },
  "packageManager": "pnpm@10.23.0",
  "devDependencies": {
    "@types/node": "^24.10.1",
    "del-cli": "^7.0.0",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3"
  }
}
